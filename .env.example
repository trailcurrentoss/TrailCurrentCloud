# Copy this file to .env and fill in your own values
# IMPORTANT: Never commit .env to version control - it will be auto-excluded!
#
# See README.md for setup instructions

# ============================================================================
# MQTT Configuration (Mosquitto)
# ============================================================================
MQTT_USERNAME=your_mqtt_username
MQTT_PASSWORD=your_mqtt_password
# ^ Plain passwords above (automatically added to mosquitto at container startup)
# ^ Both backend and mosquitto will use these same credentials
MQTT_PORT=8883

# ============================================================================
# Security
# ============================================================================
ADMIN_PASSWORD=your_admin_password_here
# ^ Plain password for system admin access

# ============================================================================
# API Configuration
# ============================================================================
API_PORT=3000
FRONTEND_PORT=8443

# ============================================================================
# Network Configuration & SSL Certificates
# ============================================================================
TLS_CERT_HOSTNAME=your_device_hostname.local
# ^ For DEVELOPMENT: Any value works (dev certs always include localhost)
# ^ For PRODUCTION: Set to your public domain (e.g., cloud.example.com)
# ^ Used by ./scripts/generate-certs.sh and ./scripts/setup-letsencrypt.sh
FRONTEND_HTTP_PORT=80
# ^ Port for HTTP (ACME challenge + redirect to HTTPS)

# ============================================================================
# Let's Encrypt (production only — ignore for local development)
# ============================================================================
LETSENCRYPT_EMAIL=
# ^ Required for Let's Encrypt certificate registration and renewal notices
# ^ Used by ./scripts/setup-letsencrypt.sh
# ^ For local development, leave blank and use ./scripts/generate-certs.sh instead

# ============================================================================
# Tileserver Map Tiles
# ============================================================================
# The Tileserver container requires a .mbtiles file for map data
# Location: data/tileserver/north-america.mbtiles
#
# See DOCS/GeneratingMapTiles.md and tileserver/generate-tiles.sh
# for instructions on generating tiles from OpenStreetMap data
TILESERVER_PORT=8080

# ============================================================================
# Environment
# ============================================================================
NODE_ENV=production
# ^ Use 'production' for normal operation
# ^ Use 'development' for debugging (enables debug port on 0.0.0.0:9229)
